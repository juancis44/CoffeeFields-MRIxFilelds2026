# =============================================================================
# MRIxFields2026 — Environment Configuration
# =============================================================================
# Copy this file to .env and edit the paths to match your environment:
#   cp .env.example .env
#   vim .env
#   source .env          # load into current shell (for bash/shell commands)
#
# Python scripts auto-load .env via mrixfields.env.load_env().
# For shell commands using $DATA_DIR etc., run `source .env` first.
#
# .env is gitignored and will not be committed.
# =============================================================================

# Root directory of the released dataset (must exist; ~150 GB)
DATA_DIR=/data/juan/MRIxFields2026_data

# Preprocessed npz directory (created by preprocess.py extract-slices; ~50 GB)
PREPROCESSED_DIR=./Baseline/preprocessed

# Root directory for training outputs: checkpoints, logs (auto-created; ~5-20 GB per experiment)
OUTPUT_DIR=./Baseline/runs

# Directory for inference predictions (auto-created; ~10 GB per task).
# Recommended: put it OUTSIDE the repo (e.g. a sibling dir) to avoid mixing
# large NIfTI outputs into the source tree.
INFERENCE_DIR=./Inference/predictions

# Directory for SynthSeg outputs (mirror tree of INFERENCE_DIR with one
# {stem}_seg.nii.gz per prediction). Task 1 / Task 2 only. Output of
# build_submission reads seg from here. Recommended a sibling of
# INFERENCE_DIR (e.g. predictions_seg/).
PREDICTIONS_SEG_DIR=./Inference/predictions_seg

# Directory for the packaged submission tree (output of build_submission;
# zip from inside each {SUBMISSION_DIR}/task{N} subdir). Recommended a sibling
# of INFERENCE_DIR.
SUBMISSION_DIR=./Submission/submissions

# SynthSeg installation path (for brain segmentation; see Evaluation/README.md)
SYNTHSEG_DIR=/home/juanc/SynthSeg

# GPU device
DEVICE=cuda:0
